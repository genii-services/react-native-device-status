{
	"name": "react-native-device-status",
	"version": "1.0.4",
	"description": "A React Native Library to check if a device is rooted or jailbroken. It also checks if screen lock is enabled",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"react-native",
		"rooted",
		"jailbroken",
		"screenlock",
		"passcode",
		"ios",
		"android",
		"secured"
	],
	"author": "appcreatier@gmail.com",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/genii-services/react-native-device-status"
	},
	"peerDependencies": {
		"react-native": "^0.27.0"
	}
}
